name: Portfolio-Tracker-Refresh

# GERUEST: Nur manueller Start. Es gibt noch kein Datensammler-Skript.
# Sobald apps/portfolio-tracker/scripts/tracker.py fertig ist, hier den
# schedule-Trigger und die echten Schritte (analog research-dashboard.yml)
# einbauen.
on:
  workflow_dispatch: {}     # nur ueber den "Run workflow"-Button

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Hinweis
        run: echo "Portfolio Tracker: noch nicht implementiert. Siehe apps/portfolio-tracker/README.md"
